<% if @mountain.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@mountain.errors.count, "error") %> prohibited this user from being saved:</h2>

    <ul>
      <% @mountain.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>